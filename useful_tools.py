import numpy as np
import matplotlib as plt

# finding values meet the requirements
y = [1,2,1,2,1,2,1]
place = np.argwhere(y == 1)


np.zeros_like
np.zeros_like(a, dtype=None, order='K', subok=True)
#Return an array of zeros with the same shape and type as a given array.